//Write a program to input two numbers and display their sum, difference, product, and quotient.
#include <stdio.h>
int main()
{
    int a, b;
    printf("enter two numbers where a is greater than b");
    scanf ("%d" , &a);
    scanf ("%d" , &b);
    int s = a+b;
    int diff = a-b;
    int pro = a*b;
    int quo = a/b;
    priintf("sum = %d /n" , s);
     priintf("difference = %d /n" , diff);
      priintf("product = %d /n" , pro);
       priintf(" quotent= %d /n" ,quo );
       return 0;
}